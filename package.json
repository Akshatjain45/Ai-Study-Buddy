{
  "name": "ai-study-buddy",
  "version": "1.0.0",
  "description": "AI Study Buddy - Full Stack App",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  }
}
